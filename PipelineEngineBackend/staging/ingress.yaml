apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
    name: backend-ingress
    namespace: pipeline-engine-staging
    annotations:
        cert-manager.io/cluster-issuer: letsencrypt-staging
spec:
    ingressClassName: nginx
    tls:
        -   hosts:
                - staging.pipeline.facture.live
            secretName: backend-tls
    rules:
        -   host: staging.pipeline.facture.live
            http:
                paths:
                    # API Path
                    -   path: /api/
                        pathType: Prefix
                        backend:
                            service:
                                name: backend-service
                                port:
                                    number: 80
